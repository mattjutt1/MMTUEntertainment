# Required environment variables for Vercel deployment

# Passcode for wife dashboard authentication (must match .ops/wife.secret)
WIFE_DASHBOARD_PASSCODE=your_secure_passcode_here

# Webhook secret for emergency halt (generate a random string)
WEBHOOK_SECRET=generate_random_32_char_string

# Optional: Your local machine webhook URL (for emergency halt)
LOCAL_WEBHOOK_URL=https://your-ngrok-or-tailscale-url/webhook